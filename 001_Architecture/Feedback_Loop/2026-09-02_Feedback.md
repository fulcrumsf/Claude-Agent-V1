## Video Generation Approval Gate
- Do not upscale or normalize any generated video until Tony has manually reviewed and approved the raw generation.
- Generation and post-processing are separate approval stages; a raw clip that needs manual review must remain raw to avoid wasting money.
- For Shot 06, the intended scene has exactly one Grandma, no second Grandma entering, no non-English dialogue, and a fully locked-off static camera.
- Shot 06 v4 failed manual review because it used multiple camera views, added unwanted "yeah yeah" dialogue, and made Grandma exit and re-enter. Future prompts must explicitly prohibit all three behaviors.
- Tony approved Shot 06 v7-first-last for passing despite a small gate defect. Manual approval is complete; post-processing may proceed, but no further video generation is needed for this shot.
- Permanent preservation rule: never overwrite any paid video generation or derivative. Every new generation, upscale, and normalized output must receive a new version number; superseded assets must be moved to the matching `Archived/` folder. Automated inspection may be wrong, so every version remains recoverable.
- Correction: storyboard generation must never bypass the structured per-frame contract and QA controller through an ad hoc direct image-generation prompt. Every frame must explicitly establish subject origins, spatial geometry, transitions, and physical cause; the generated sheet must be checked against that contract before it is presented or handed to Seedance. A memory entry or skill paragraph alone is insufficient enforcement.
