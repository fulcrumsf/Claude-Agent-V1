---
title: Feedback Log — 2026-04-29
type: feedback
tags: [feedback]
---

# Feedback Log — 2026-04-29

## [Validated] Ask clarifying questions before building system-wide tools

Tony explicitly asked for clarifying questions before starting the System Map build. He gave detailed, thoughtful answers. This was the right call — the scope (everything on the Mac, not just workspace), the Python script depth (one-liner, not full docstrings), the auto-update mechanism, and the Docker detail level were all non-obvious and needed confirmation.

**Rule:** For workspace-wide or system-wide tools, always ask clarifying questions first even if the task seems clear. The "what does this cover" question in particular is easy to get wrong.

---

## [Validated] Use local cron, not remote scheduled agents, for local-system tasks

When user asked to auto-update the system map, I correctly identified that remote CCR agents cannot access the local Mac filesystem and redirected to local crontab. Tony did not push back — this was the right call.

**Rule:** Before creating a remote scheduled agent, ask: does this task require local filesystem, local CLIs (brew, docker, etc.), or local env vars? If yes → local crontab, not remote CCR.

---

## [Validated] Iterative scan refinement before finalizing the map

The initial scan returned 1,995 Python scripts (mostly library files) and 1,663 "skills" (every .md in the plugin directory). Tony did not complain but I self-corrected. After filtering:
- Python scripts: 1,995 → 268
- Skills: 1,663 → 17

**Rule:** When a scan returns unexpectedly large numbers, investigate the source distribution before writing the final output. A quick `Counter()` on path prefixes instantly revealed the issue.
