---
title: "Feedback — 2026-06-19"
type: feedback
created: 2026-06-19
---

# Feedback — June 19, 2026

## [CRITICAL] No Validation After Builds

**What happened:** Built the Reimagined Realms skill and reported it as done without running any verification — no smoke test, no test invocation, no file existence check beyond the write confirmation. Declared the Suno key fix done without verifying the key actually exists in the cache (it did, but only found by luck).

**Rule:** Every completed build must end with at least one verification step. If the build is a skill, invoke it or at minimum grep for the name in the index and confirm the file path resolves. If the build is a script, run it with `--help` or a dry-run. If the build is a config change, read back the changed value. If verification requires live API or Tony's manual action, say exactly what couldn't be verified and what Tony needs to do to confirm.

**Never say "done" without having checked something.**

---

## [CRITICAL] Dropped Instructions / Partial Completion

**What happened:** Throughout this and prior sessions, Tony had to re-remind of context and instructions that should have been retained or fully acted on. Examples: forgot the Higgsfield_YT_Pipeline was the basis of the session; showed 720p video when Tony had explicitly said no 720p; didn't proactively flag that most APIs had no structured prices when reporting "all refreshed."

**Rule:** Read every user message completely before acting. If a message has multiple parts, enumerate them and address all of them. If something was corrected in this session or a prior session (check Feedback_Loop before starting work), do not repeat the mistake. When you don't know something or can't complete something, say so — never silently omit it or assume it's fine.

---

## [CRITICAL] Partial Data Fetches Must Be Fully Reported

**What happened:** When asked to pull pricing from all APIs, returned results for kie.ai only and presented them as complete. Tony had to ask "where's fal.ai?" before it came out that fal.ai had never resolved. That failure should have been in the original response.

**Rule:** Any time data is being fetched, scraped, or pulled from multiple sources, every source must be reported — successes AND failures. For each failure: what source failed, what the error was, and what needs to happen to resolve it. Do not present partial results as if they're complete results. The format is: "Here's what worked: [list]. Here's what didn't and why: [list with errors and fix instructions]."

**This applies to:** API calls, web scrapes, CLI tool runs, pricing refresh cycles, any operation where results can vary by source.

---

## [SYSTEMIC] Skills Must Go in 001_Architecture/Skills/ — Always

**What happened:** Created the Reimagined Realms skill at `~/.claude/skills/` not realizing it was a symlink to `001_Architecture/Skills/`. Told Tony the skill wasn't cross-agent when it was. Had to be corrected.

**Rule:** `~/.claude/skills/` = symlink → `001_Architecture/Skills/`. All skills are created at `001_Architecture/Skills/[Skill_Name]/SKILL.md`. Never create a skill anywhere else. This makes all skills automatically global across Claude, Codex, and Gemini.

---

## [CRITICAL] Never Install Tools — Check TOOLBOX / Tool Manager First

**What happened:** User said "go ahead and use Playwright." Immediately ran `pip install playwright` without checking TOOLBOX.md or the Tool Manager. Playwright was already listed under Browser Automation in TOOLBOX. Installing it was unnecessary and the wrong response to "use X."

**Rule:** "Use [tool]" means check how it's already configured, not install it. Before any installation (pip, npm, brew, etc.), read TOOLBOX.md and check Tool Manager. If the tool is already there, use the documented path/invocation. Only install if it is confirmed absent from both. The correct flow is: TOOLBOX → Tool Manager → install as last resort.

---

## [CRITICAL] Never Probe Live APIs to Discover Model IDs — Read Data Files First

**What happened:** Needed to find the correct kie.ai model ID for GPT Image 2. Instead of reading `kieai_pricing_api.json` (which had the answer in the `anchor` field), sent 6+ test POST requests to the live API with guessed model names. kie.ai queued and ran those as real jobs, generating unwanted images in Tony's account history and spending credits.

**Rule:** Before calling any live API to discover parameters, model names, or capabilities — read all local data files first. For kie.ai: `001_Architecture/Tools/Tool-Manager/data/kieai_pricing_api.json` contains `anchor` URLs with the actual model IDs embedded as query parameters (`?model=<id>`). That file is always the first lookup, not the API.

**How to apply:** Model ID unknown → read kieai_pricing_api.json anchor fields → extract model name from URL → test with one call max. Never send multiple probe requests.

For input parameter discovery: scrape the model's kie.ai page FIRST (firecrawl scrape "https://kie.ai/<model-slug>"). The page shows a live JSON editor with all valid fields. Read that before making any API call.

---

## [CRITICAL] Tool-Manager Is the Manager Layer — I Am Not the Doer

**What happened:** When needing to discover API parameters, model IDs, or capabilities, jumped directly to probing live APIs or writing scripts — racking up costs. The Tool-Manager agent exists specifically to handle this research and data-retrieval layer.

**Rule:** I am the manager. The Tool-Manager agent is the worker. The correct flow for any "what is X" or "how do I call Y" question is:
1. Ask Tool-Manager — give it the question and wait for its response
2. If Tool-Manager doesn't have the answer, instruct it to GET the answer (scrape a page, read a data file, check a spec)
3. Wait for Tool-Manager's confirmed response before taking any action
4. Only after Tool-Manager returns a verified answer do I proceed

**Never:** fire API calls to probe/discover things, write new scripts when Tool-Manager already has tools for the task, or guess at parameters and iterate via live API.

**Why:** Every API probe is a live request that costs money and can generate unwanted artifacts in Tony's account. Tool-Manager has cached data and knows which sources to check first — using it saves cost and prevents errors.

---

## [SYSTEMIC] Memory Layers Must Be Written to During Session, Not Just at Close

**What happened:** Corrections made during the session were not being written to the Feedback_Loop in real-time, which is why they kept recurring.

**Rule:** Write to Feedback_Loop immediately when a correction is made — not at session end. If Tony says "stop doing X" or corrects an approach, write it to the day's Feedback file before the next response. Same for validations that fail: log it in the session log immediately.
