# Feedback Log — 2026-08-09

## Always back up superseded assets to a "Rejected" folder before overwriting

**Correction:** When regenerating Titanic Stoker shots 2, 9, 13 with corrected first-frame images, Tony noted that overwriting the old `Shot_02.mp4`/`Shot_09.mp4`/`Shot_13.mp4` video clips is fine, but the old versions should always be backed up to a `Rejected` folder first, not silently replaced. He handled this instance himself, but the rule is standing.

**Why it matters:** This generalizes the manual `Rejected` folder convention he set up earlier in the Roman Gladiator production (a no-go zone for images he drags rejected ones into) into a proactive rule I should follow: any time I'm about to overwrite an existing generated asset (image or video) with a corrected regeneration, move the old file into a sibling `Rejected/` folder before writing the new one, rather than letting it be silently lost.

**How to apply:** Going forward, whenever regenerating/replacing an asset at an existing path (not just when Tony flags it), check whether the target already exists — if so, move it into a `Rejected/` subfolder alongside it (creating the folder if needed) before writing the new version. This applies workspace-wide, not just to this production.

**Status:** Applied as standing behavior going forward.
