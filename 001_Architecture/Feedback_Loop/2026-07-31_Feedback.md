---
title: "Feedback Loop — 2026-07-31"
type: feedback-log
created: 2026-07-31
---

# Feedback Loop — 2026-07-31

**Correction — trust the director on footage intent, but verify what you can first.** Tony corrected two vision-analysis misreads on the Glass Guard shoot: (1) claimed grime at IMG_9100 7s/41s that I couldn't confirm even at full resolution — he explained it's too subtle for a text description to convey to a vision model, and I should trust his read of his own footage; (2) IMG_9184 read as empty ceiling-only footage to both the vision model and my own frame checks — it was actually shot through now-transparent glass, so "looks empty" was itself the signal the product worked. He said explicitly: "I am the director." **Pattern going forward:** for anything about what raw footage actually shows/means, do one round of independent verification (pull frames, check timestamps), but when the creator insists after that and gives a plausible explanation for the discrepancy, defer to them rather than pushing back again — don't make him repeat the correction.

**Correction — never hard-cut audio at silence boundaries.** Naive `atrim` + `concat` at detected silence_start/silence_end produces audible clicks (no crossfade at the discontinuity) and clipped words (detector threshold can trigger inside a consonant). Fix: keep a safety-padding margin of real audio on both sides of every cut, plus a short fade at every join. Verify with a raw-PCM sample-jump scan (should be zero) and a re-transcription diff (no words lost). Now permanent as `trim_vo_pauses.py` / SKILL.md Step 5a.4 — see [[feedback_vo_pause_trimming]] in Claude cross-session memory.

**Explicit ask — stop making Tony repeat pipeline-level fixes.** After approving the audio fix, Tony said directly: "I don't want to have to keep telling you in newer videos... how to fix the voiceover." This is a general instruction, not specific to this bug: when a real defect is found and fixed during a production, the fix belongs in the skill/script itself (not just in this session's output) so it's automatic on the next run, without being asked. Applied immediately by promoting the trim script and editing SKILL.md in the same session, before session close.

**Preference confirmed — single-video mode is a real, supported option.** Tony explicitly opted out of the standard 3-variant A/B TikTok Shop set for Glass Guard ("this will only be for one video... I'm not going to do three separate videos"). Don't default to 3 outputs without confirming per-product.

**Preference confirmed — verify live state before trusting a cached ID/value, even mid-session.** Caught myself about to post to Blotato using a memory-recalled account ID before verifying live — did the live `blotato_list_accounts` check first per the account-ID-drift rule already in memory, which surfaced a real mismatch (I'd conflated NeonParcel's YouTube and TikTok account IDs). No correction needed from Tony here, but worth reinforcing: this rule paid off in practice.
