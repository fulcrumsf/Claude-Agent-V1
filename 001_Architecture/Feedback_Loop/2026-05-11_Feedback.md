# Feedback Log — 2026-05-11

## [Workspace Rename Must Be Canonical]

**What happened:** Tony confirmed the workspace should no longer be referred to by the old workspace name anywhere in Antigravity or future workspace-facing docs.

**Tony's correction:** Use `Agent-OS` as the only visible workspace name. Do not leave the old name in Explorer, recent-folder state, or user-facing labels once the transition is complete.

**Action taken:** Refreshed the Antigravity workspace cache so the Explorer root resolves to `Agent-OS`.

**What Tony clarified:** `000_Ingest/` should never be graphified. It remains the landing zone for incoming files, but Graphify should skip it entirely.

**Why it matters:** `000_Ingest/` is a temporary dump area for unsorted files. Graphify should only cover files after ingest routes them into durable destination folders like `007_Resource_Library/` or `000_Wiki/`.
