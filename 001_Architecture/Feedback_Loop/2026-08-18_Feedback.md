# Feedback — 2026-08-18

**Declared a video-generation defect "confirmed fixed" after checking only 2 of 8 dense keyframes — Tony caught it directly.** On the Seedance 1.5 Pro v4 test clip (Scene 01, mantis shrimp), I read `Dense_Keyframes/0001.jpg` and `0002.jpg`, saw correct compound-eye anatomy in both, and reported the eye-anatomy fix as confirmed working. Tony pointed out that watching further into the clip, the creature morphs into something alien-looking — two mechanical antennae sprout from the head and the eyes regain human-like round pupils by frame 6. I re-checked `0006.jpg` and confirmed he was right. Why it matters: this is exactly the failure mode the Video-Analyzer skill's own mandatory step exists to prevent ("the invoking agent MUST use the Read tool to view every file in Keyframes/ directly... not the model's description of them") — I read a partial sample and treated it as equivalent to a full review, then stated a conclusion with more confidence than the evidence supported. How to apply: when a skill's own instructions say "review every keyframe," that means every keyframe, not a representative-looking sample from early in the clip — defects that appear only later in a shot (drift, morphing, degradation) are the exact class of thing a partial check misses.

**Validated approach — "A+" from Tony (2026-08-18): component-asset generation + Remotion keyframed compositing, instead of Seedance video generation, for diagram/motion-graphics-style animated beats.** When Seedance 1.5 Pro hallucinated badly trying to animate Scene 02's photoreceptor-comparison diagram (start+end frame only — morphed into an unrelated mechanical/alien structure within 2 seconds, never converged on the actual end frame), the fix was to stop asking a video-generation model to invent the in-between motion at all: generate each distinct visual element as its own clean, isolated static asset (Diagram-Generation skill's "Approach B"), then composite/animate them in Remotion with keyframed opacity/scale/position tied to real narration timestamps. Zero drift by construction, since nothing is being regenerated frame-to-frame — confirmed by rendering and reviewing frames across the full timeline against the original storyboard panels, pixel-exact matches throughout. Why it matters: for abstract/diagram/data-viz content specifically (as opposed to organic creature footage), a generative video model is the wrong tool even when given "correct" start/end frame references — Approach B should be the default assumption for this content type, not a fallback only reached for after a failed Seedance attempt. This is being turned into a proper reusable skill next.

**Second validated fix within the same session: true alpha mattes over mix-blend-mode tricks for compositing layered static assets.** First fix attempt (`mixBlendMode: "screen"`) removed the hard black seam between crossfading assets but left a faint grayish residual line, because the assets' baked-in "isolated on solid near-black background" wasn't literally pure black (`#050b10`, not `0,0,0`) — screen blend still adds brightness wherever two non-zero colors overlap. Real fix: ran each asset through Recraft's AI background-removal model (`kie-cli recraft_remove_background`), confirmed true `RGBA` output, and reverted to plain alpha-channel compositing (no blend-mode hack, which would incorrectly wash out any frame where two subjects genuinely overlap). Zero seam, any transition point. Lesson: a blend-mode workaround on a flat-background asset is a visible-artifact-reduction hack, not equivalent to real alpha transparency — when a clean isolated asset is genuinely needed for layered compositing, matte it properly rather than fighting a solid background with clever blending.

**Correction, direct and firm from Tony: defaulted to kie.ai's GPT-Image-2 endpoint without ever consulting Tool-Manager, and Tony is "sick of asking you to ask the Tool Manager."** Built today's component assets via kie.ai's `gpt-image-2-image-to-image` purely by pattern-matching from other scripts in the codebase — never checked whether kie.ai's wrapper actually supports what the job needed (transparent-background output; it doesn't — confirmed its CLI schema has no background/transparency parameter at all, while direct OpenAI's API does). On price alone kie.ai happened to be right ($0.03 vs $0.04/image), but that's not the point: the real gap Tony flagged is that Tool-Manager should be consulted *before* defaulting to any platform, without being asked, every time — not just when a price question comes up. **What I'm changing:** query Tool-Manager before any generation-platform choice, unprompted. When Tool-Manager's own data doesn't cover the actual question, instruct Tool-Manager to research and update its own data via its documented Update Protocol (`⚠️ TOOLBOX UPDATE NEEDED`) — never surface an unresearched question back to Tony as if it's his job to already know the answer. Concretely fixed today: added a `capabilities` field to `model_catalog.json` (feature/platform parity, not just price) so this specific gap can't recur for GPT-Image-2 transparency, and updated Tool-Manager's own routing rule to check capabilities before price.

**Operational finding: wavespeed CLI's video upload aborts on larger files, no clear size threshold documented.** A small (35KB) test clip uploaded fine; the real 17.5MB/11s Scene 02 render consistently failed with `"This operation was aborted"` across 3 retries — not transient. Fix: compress before upload (960px width, crf 28) — a 687KB version uploaded and processed successfully. Worth checking wavespeed's actual documented size/timeout limits before hitting this again on a longer or higher-resolution clip; not yet root-caused beyond "smaller worked."

**Separate finding, not yet resolved: Mirelo's video-to-SFX output was capped at 5.0s against an 11.05s input clip**, with no error or truncation warning — just silently returned a shorter file. Worth checking Mirelo's documented max output duration (or whether `sonilo`, the swappable alternative in `foley_config.py`, handles longer clips) before relying on it for any beat longer than ~5s.
