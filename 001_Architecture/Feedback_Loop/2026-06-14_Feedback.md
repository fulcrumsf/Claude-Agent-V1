# Feedback Log — 2026-06-14

## [CRITICAL] Never Delete, Remove, or Move Files
- **What happened:** Offered to delete the `Claude-agent` remnant folder. Tony immediately stopped me.
- **Rule:** Never delete, remove, rename, or move any files or folders under any circumstances. A prior agent deletion wiped Tony's entire workspace and cost months of recovery. Tony handles all destructive operations himself.
- **Applies to:** Every agent, every situation, even when something is explicitly identified as stale/safe to remove.

## [CRITICAL] API Keys in Config Files
- **What happened:** Found a live GCP API key hardcoded in `gemini_mcp_config.json`. Tony was frustrated — this rule has been stated many times.
- **Rule:** Live API keys must NEVER appear in any file. `~/.env-secrets` is the one and only source of truth. Config files always use `${KEY_NAME}` placeholders. This applies to every file type: `.json`, `.yaml`, `.toml`, `.env`, shell scripts, Python scripts, everywhere.
- **Note:** `~/.mcp-secrets.env` does not exist and must never be referenced.

## [CORRECTION] Universal Memory Must Go to Global_Agent_Memory.md First
- **What happened:** I saved the API key rule only to Claude Code-specific memory (`~/.claude/.../memory/`). Tony pointed out it needs to be in universal/global memory that all agents can read.
- **Rule:** When saving any durable memory, always write to `001_Architecture/Memory/Global_Agent_Memory.md` first. Agent-specific memory (Claude Code's MEMORY.md, etc.) is secondary and optional.

## [PREFERENCE] Tony Does Not Want Unnecessary Confirmation Questions
- When investigation reveals something is safe and the path forward is clear, proceed. Don't offer to do things Tony said to do and then ask again if he's sure.
- Exception: destructive operations — always stop and report, never proceed.
