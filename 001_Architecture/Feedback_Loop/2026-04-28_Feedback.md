---
title: "Feedback — 2026-04-28"
type: doc
domain: architecture
tags: [doc, architecture]
---

# 2026-04-28 — Feedback

## Resume sessions from saved plans, not from memory assumptions

**What happened:** I opened the session by assuming we'd resume CLAUDE.md migration work (based on a memory entry). Tony corrected: "that's not where we left off" — the actual context was a saved plan at `~/.claude/plans/partitioned-spinning-patterson.md` (Wikify + Graphify).

**Rule:** On "let's start where we left off" or similar resume phrasing, check `~/.claude/plans/` for recent plan files **before** trusting memory entries. Memory captures intent at write time but may not reflect the most recent direction.

**How to apply:** First step on any resume request — `ls -lt ~/.claude/plans/ | head` and skim the freshest one for relevance.
