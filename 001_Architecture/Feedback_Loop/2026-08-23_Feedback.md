# 2026-08-23 Feedback Log

## Anomalous Wild Is Entertainment-First, Not Documentary-Grade

Shown a confirmed biology/continuity error in the Mantis Shrimp Color Vision video (0002) — a single fused raptorial appendage at 1:32–1:37 that morphs into a completely wrong lobster-claw type at 1:43–1:46, contradicted by a correct shot 13 seconds later — Tony declined the fix: "it's educational but it's really for entertainment so I'm not gonna get too crazy on the specific scientific stuff."

**Why:** the channel's bar is entertainment value, not documentary-grade accuracy, once content is at final review.

**How to apply:** keep flagging scientific/continuity errors during review on this channel — Tony wants visibility into what's wrong — but don't treat an unaddressed accuracy issue as a blocker or push for a mandatory fix once he's seen it and made the call.

## Video-Analyzer Skill Gap — No Local File Support

`analyze_reference_video.py` only accepts YouTube URLs (via yt-dlp download) — there is no path for analyzing a local video file directly. To run it against the locally-assembled `FINAL_v1.mp4`, the workaround was to call the skill's internal Python functions directly rather than going through its normal CLI entry point.

**Why:** the skill was built assuming the source is always a published YouTube video, but in-production review needs to analyze a local assembly before it's ever uploaded.

**How to apply:** until the skill is updated with a local-file path, expect to work around it the same way (call internal functions directly) any time a pre-publish local assembly needs Video-Analyzer's scene-detection/transcript pass. Worth fixing properly in the skill itself if this recurs.

---

## Scene 03 Rebuild Candidate — Graded B+/A-

Tony reviewed `Assembly/0002_Mantis_Shrimp_Color_Vision_FINAL_v2_candidate.mp4` (B-roll removal + full-length raw restore + freeze-hold gap close) and gave it a **B+/A-**, up from the B-/B on the original FINAL_v1 cut.

**Why:** Confirms the B-roll removal approach (drop stock cutaways, restore full raw clip length, close the gap with a freeze-hold rather than a crossfade) was the right call — validated, not corrected. Worth repeating as the default fix pattern when stock B-roll needs to come out of an Anomalous Wild assembly and raw footage exists to fill the gap.

**How to apply:** Next time raw/unused footage exists for a scene where stock B-roll is being cut, default to: restore full raw clip length first, freeze-hold only the remainder that doesn't close naturally. Don't reach for crossfade to solve a duration shortfall — it eats existing frames and makes the gap worse.

## Self-Correction — Overwrote This File Instead of Appending

Used the Write tool with a placeholder comment ("see earlier entries") instead of reading the existing file and appending, destroying this morning's two real entries above (reconstructed from the Claude memory nodes that had already captured them separately — no data lost, but it shouldn't have happened).

**Why:** CLAUDE.md's Session Memory rule is explicit — "one per day, append entries." Write overwrites; it should only be used for daily logs after confirming via Read what's already there, or Edit should be used instead.

**How to apply:** for any file governed by an append-only convention (daily session logs, daily feedback logs), always Read first and use Edit to append, never Write with a summary placeholder standing in for content that already exists on disk.
