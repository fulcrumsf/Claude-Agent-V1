---
title: "2026-09-05 Feedback"
type: feedback-log
created: 2026-09-05
---

# 2026-09-05 Feedback

## Graphify never touches images/videos — harden it, stop re-learning it

Tony, visibly frustrated: "this is a rule that you should already know with Graphify
because I've said it many times. I don't know why it's not hardened into your brain."

**Rule:** images and videos NEVER get graphified — any format, any case. They get
ingested → converted to a wiki note (what it is, its style e.g. Bauhaus/abstract,
what it was used for) with descriptive tags → cross-referenced through the WIKI, not
the graph.

**Why it matters:** repeated misses waste his time re-explaining. The rule was in
memory but framed around the `.graphifyignore` mechanism, not the workflow intent.

**How to apply:** when any graphify scope question comes up, state the images/videos
exclusion up front as settled, don't re-derive it. Memory updated
(`feedback-graphify-text-code-only`, `project-resource-library-purpose`).

## Resource Library graphify — Tony wants it done a specific way

Do NOT build the `007_Resource_Library` graph freelance. Tony has a specific method
he'll walk through. Agreed prep: version-check graphify → build a de-dup script
(he re-bookmarks things, esp YouTube tutorials) → re-tag legacy notes to the new
frontmatter contract → then graph. He validated the whole analysis + the hardened
frontmatter contract without pushback.

## Deferred-work discipline

When Tony says "let's do this later," capture it in the handoff + a memory with the
exact trigger condition ("after the Neon Parcel video"), not just a vague note.
Confirmed good this session.

## Neon Parcel Shot 12 storyboard correction

- Tony identified that Shot 12 v2 did not establish Grandma's route: she appeared
  near the trampoline without a clear gate-to-steps transition, encouraging
  Seedance to duplicate or teleport her.
- New storyboard rule reinforced: each panel's visual area must be 16:9 and the
  wide composition must show the subject origin, continuous route, intermediate
  ground position, obstacle/steps, and final destination before video generation.
- Attempt 1 was preserved for audit. Attempt 2 was generated and visually checked:
  gate threshold -> open grass -> bottom of lower-left steps -> climb -> trampoline.
  It is pending Tony approval; no Seedance generation is authorized.

- Tony prefers the frame showing Grandma climbing the steps over the frame showing
  her standing on the trampoline. The standing pose makes her oversized relative
  to the trampoline and cubs; use the climbing frame as the stronger physical and
  scale reference, or revise the final frame to preserve natural proportions.

## Neon Parcel Shot 12 v3 generation

- Tony authorized video generation from the climbing-step storyboard direction.
- Preserve the raw v3 as a separate paid artifact. Do not upscale before Tony's
  manual approval. Current review concern: the generated video may not visibly
  follow the storyboard's lower-left-step route even though no duplicate Grandma
  was reported by the advisory Gemini inspection.
- Status correction: v3 did not pass. The six storyboard panels define six distinct
  spatial/action checkpoints, and the generated video did not realize that complete
  progression. Never describe it as ready/pass when the storyboard checkpoints are
  not visibly present.

## Neon Parcel Shot 12 tiled-storyboard correction

- Direct vision review must inspect whether a generated video has reproduced the
  storyboard sheet itself as a tiled/contact-sheet layout. Gemini's earlier semantic
  notes were insufficient for this failure mode.
- Composite storyboard sheets are planning/QA artifacts only for the Kie Seedance
  Mini route unless a provider-specific live test proves safe temporal conditioning.
  Use clean, separately approved 16:9 start/end frames for the paid video call.

## Explicit Seedance image-reference tags

- Kie's Seedance Mini playground example establishes the provider-specific
  syntax as `@Image 1`, `@Image 2`, etc., matching upload order. Use that exact
  Kie syntax for the controlled test; do not confuse it with generic guidance
  using `@image_1` or `@Image1`.

## Validated Kie storyboard prompt pattern

- Tony approved the Shot 12 v6 result: explicit `@Image 1` storyboard binding
  produced one continuous shot with one camera angle and correctly followed the
  storyboard sequence. This is now the preferred Seedance storyboard prompt
  structure for Kie, subject to provider/version scope.
- Lock the prompt header and sequence format: `Shots: 1 | Duration: N seconds |
  Aspect Ratio: 16:9`, explicit reference mapping, `Shot 1, panel N`, and a
  direct instruction to animate panels chronologically without reproducing the
  sheet.

## All visual references require explicit roles

- Tony clarified that every uploaded visual reference must be explicitly tagged
  in the generated Seedance prompt by upload order: storyboard, character sheet,
  environment sheet, prop sheet, and any later image must each receive its own
  `@Image N` role. Wispr Flow dictation is intent input; the pipeline must emit
  exact provider syntax and preserve the mapping in the manifest.
## Shot 12 storyboard endpoint correction

- Tony identified that the successful video still stopped at the stairs because
  the storyboard ended with Grandma climbing, not visibly on the trampoline and
  beginning to jump.
- Revised storyboard requirement: the final panels must show arrival on the
  trampoline and the first small bounce, with Grandma naturally scaled beside
  the cubs. Do not use an oversized standing pose as the endpoint.

## Shot 12 frame-5 continuity correction

- Tony approved storyboard v4 for video generation, with one important motion
  note: in frame 5 the same Grandma should remain visible while following the
  curved route down the trampoline's left side. She should still face the fixed
  camera, turned only slightly counterclockwise toward the lower-left steps.
- The v8 contract and Seedance prompt must bind this as a continuity constraint:
  no duplicate Grandma, no left-edge re-entry, no unexplained repositioning,
  and no ending before the visible trampoline arrival and first bounce.
