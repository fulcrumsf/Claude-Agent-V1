---
title: "2026-09-05 Feedback"
type: feedback-log
created: 2026-09-05
---

# 2026-09-05 Feedback

## Graphify never touches images/videos — harden it, stop re-learning it

Tony, visibly frustrated: "this is a rule that you should already know with Graphify
because I've said it many times. I don't know why it's not hardened into your brain."

**Rule:** images and videos NEVER get graphified — any format, any case. They get
ingested → converted to a wiki note (what it is, its style e.g. Bauhaus/abstract,
what it was used for) with descriptive tags → cross-referenced through the WIKI, not
the graph.

**Why it matters:** repeated misses waste his time re-explaining. The rule was in
memory but framed around the `.graphifyignore` mechanism, not the workflow intent.

**How to apply:** when any graphify scope question comes up, state the images/videos
exclusion up front as settled, don't re-derive it. Memory updated
(`feedback-graphify-text-code-only`, `project-resource-library-purpose`).

## Resource Library graphify — Tony wants it done a specific way

Do NOT build the `007_Resource_Library` graph freelance. Tony has a specific method
he'll walk through. Agreed prep: version-check graphify → build a de-dup script
(he re-bookmarks things, esp YouTube tutorials) → re-tag legacy notes to the new
frontmatter contract → then graph. He validated the whole analysis + the hardened
frontmatter contract without pushback.

## Deferred-work discipline

When Tony says "let's do this later," capture it in the handoff + a memory with the
exact trigger condition ("after the Neon Parcel video"), not just a vague note.
Confirmed good this session.

## Neon Parcel Shot 12 storyboard correction

- Tony identified that Shot 12 v2 did not establish Grandma's route: she appeared
  near the trampoline without a clear gate-to-steps transition, encouraging
  Seedance to duplicate or teleport her.
- New storyboard rule reinforced: each panel's visual area must be 16:9 and the
  wide composition must show the subject origin, continuous route, intermediate
  ground position, obstacle/steps, and final destination before video generation.
- Attempt 1 was preserved for audit. Attempt 2 was generated and visually checked:
  gate threshold -> open grass -> bottom of lower-left steps -> climb -> trampoline.
  It is pending Tony approval; no Seedance generation is authorized.

- Tony prefers the frame showing Grandma climbing the steps over the frame showing
  her standing on the trampoline. The standing pose makes her oversized relative
  to the trampoline and cubs; use the climbing frame as the stronger physical and
  scale reference, or revise the final frame to preserve natural proportions.

## Neon Parcel Shot 12 v3 generation

- Tony authorized video generation from the climbing-step storyboard direction.
- Preserve the raw v3 as a separate paid artifact. Do not upscale before Tony's
  manual approval. Current review concern: the generated video may not visibly
  follow the storyboard's lower-left-step route even though no duplicate Grandma
  was reported by the advisory Gemini inspection.
- Status correction: v3 did not pass. The six storyboard panels define six distinct
  spatial/action checkpoints, and the generated video did not realize that complete
  progression. Never describe it as ready/pass when the storyboard checkpoints are
  not visibly present.

## Neon Parcel Shot 12 tiled-storyboard correction

- Direct vision review must inspect whether a generated video has reproduced the
  storyboard sheet itself as a tiled/contact-sheet layout. Gemini's earlier semantic
  notes were insufficient for this failure mode.
- Composite storyboard sheets are planning/QA artifacts only for the Kie Seedance
  Mini route unless a provider-specific live test proves safe temporal conditioning.
  Use clean, separately approved 16:9 start/end frames for the paid video call.

## Explicit Seedance image-reference tags

- Kie's Seedance Mini playground example establishes the provider-specific
  syntax as `@Image 1`, `@Image 2`, etc., matching upload order. Use that exact
  Kie syntax for the controlled test; do not confuse it with generic guidance
  using `@image_1` or `@Image1`.

## Validated Kie storyboard prompt pattern

- Tony approved the Shot 12 v6 result: explicit `@Image 1` storyboard binding
  produced one continuous shot with one camera angle and correctly followed the
  storyboard sequence. This is now the preferred Seedance storyboard prompt
  structure for Kie, subject to provider/version scope.
- Lock the prompt header and sequence format: `Shots: 1 | Duration: N seconds |
  Aspect Ratio: 16:9`, explicit reference mapping, `Shot 1, panel N`, and a
  direct instruction to animate panels chronologically without reproducing the
  sheet.

## All visual references require explicit roles

- Tony clarified that every uploaded visual reference must be explicitly tagged
  in the generated Seedance prompt by upload order: storyboard, character sheet,
  environment sheet, prop sheet, and any later image must each receive its own
  `@Image N` role. Wispr Flow dictation is intent input; the pipeline must emit
  exact provider syntax and preserve the mapping in the manifest.
## Shot 12 storyboard endpoint correction

- Tony identified that the successful video still stopped at the stairs because
  the storyboard ended with Grandma climbing, not visibly on the trampoline and
  beginning to jump.
- Revised storyboard requirement: the final panels must show arrival on the
  trampoline and the first small bounce, with Grandma naturally scaled beside
  the cubs. Do not use an oversized standing pose as the endpoint.

## Shot 12 frame-5 continuity correction

- Tony approved storyboard v4 for video generation, with one important motion
  note: in frame 5 the same Grandma should remain visible while following the
  curved route down the trampoline's left side. She should still face the fixed
  camera, turned only slightly counterclockwise toward the lower-left steps.
- The v8 contract and Seedance prompt must bind this as a continuity constraint:
  no duplicate Grandma, no left-edge re-entry, no unexplained repositioning,
  and no ending before the visible trampoline arrival and first bounce.

## Neon Parcel narration timing and voice preference

- Tony selected ElevenLabs voice `Herbie` (`Kz0DA4tCctbPjLay2QT1`) for the
  compilation narrator.
- Narration should be short, sweet, and used primarily as brief introductions
  or transitions. Preserve usable dialogue and spontaneous speech already
  present in the generated clips.
- Voiceover must not narrate continuously over the footage: each VO segment
  should be shorter than its associated clip, and total narration should remain
  substantially shorter than the combined clip runtime to preserve editorial
  wiggle room.

## Validated Neon Parcel audio review cut

- Tony approved the narration direction after reviewing the corrected v4 cut:
  short Herbie introductions and transitions add humor while leaving the
  original clip sound available.
- Lock the review mix defaults for this pipeline: separate shot-aligned VO
  assets, original audio volume `0.55`, narration volume `1.6`, 48 kHz stereo,
  timestamp-safe filtered concat, intentional silence for clips with no audio,
  and no music or branding before narration review.
- Always verify that the audio stream reaches the same endpoint as the video;
  the earlier review renders exposed why stream-copy concat is unsafe here.

## Neon Parcel music and end-screen direction

- After narration approval, Tony wants an instrumental Suno-style music bed
  that feels comical, quirky, whimsical, kid-friendly, and family-friendly,
  like a funny home-video television show. Avoid lyrics and commercial or
  dramatic scoring.
- Every long-form Neon Parcel video must end with the horizontal seven-second
  asset at `002_Content-Creation/Video_Editor/002_Channels/002_Neon-Parcel/Assets/Neon_Parcel_Endscreen_Horizontal_1080.mp4`.
- CTA narration must be generated with Herbie and fit completely inside the
  seven-second end screen. Keep the CTA simple and wait for Tony to select the
  wording before generating it.

## Neon Parcel production status

- Tony graded the completed compilation **B** and set the Neon Parcel
  Compilation pipeline at **65% complete/autonomy-ready**.
- At **95%**, Tony intends to run it mostly autonomously on a schedule with
  minimal iteration and interaction. Until then, keep manual creative review
  and package approval active.
- The next gated phase is the title, description, thumbnail, and complete
  YouTube package. Do not publish before Tony approves that package.

## Video-Analyzer routing and frame-sensitive QA

- Tony approved explicit analysis categories instead of one generic video pass.
  `case-study` and `tutorial` use Gemini agentic video understanding; `continuity`
  and `physics` use static Gemini plus dense 0.5-second keyframes; `screen-text`
  uses static Gemini plus full-resolution scene-cut keyframes; `hybrid` combines
  agentic narrative analysis with dense keyframes.
- Frame-sensitive QA must explicitly look for deformed or extra limbs, morphing,
  duplicate or disappearing people/animals, broken object connections,
  impossible entrances/exits, and actions occurring before their physical cause.
- Agentic mode is a supplement, not a replacement, for dense sampling when the
  question requires exhaustive frame-level evidence. The analyzer records
  whether Gemini returned `processing_call` and `processing_result` trace steps.

## Thumbnail tutorial case study

- The tutorial's transferable principles are additive only: make a structured
  thumbnail architecture brief, identify one dominant visual promise, plan
  negative space and mobile readability, use modular assets for complex or
  identity-sensitive subjects, and refine one variable at a time.
- Do not import creator-face shock reactions, floating software logos, vendor
  lock-in, or unverified viral/CTR claims into Neon Parcel by default. The
  thumbnail skill remains unchanged until Tony approves the add-on.

## YouTube thumbnail skill add-on approved

- Tony requested the tutorial takeaways be added as an optional mode without
  overwriting existing thumbnail logic. Added `Architecture-First Thumbnail
  Pass` to the YouTube thumbnail skill as a complementary workflow.
- The mode adds visual-promise selection, a structured architecture brief,
  modular-asset decisions, one-variable refinements, and mobile/safe-zone QA.
  Existing generation patterns, specifications, checklist, and A/B testing stay
  intact.

## Optional thumbnail mode applied to compilation

- Tony asked to implement the new optional thumbnail practice against the
  current Grandma-and-Bear compilation and show three examples.
- Validated approach: keep the practice architecture-first and additive; use
  one dominant promise, clear spatial hierarchy, mobile-safe text space, and
  complete prompts before spending generation credits.
- Saved three options in the production package and held generation pending
  Tony's choice.

## Thumbnail candidates generated

- Tony authorized generation of all three architecture options. Each was
  generated independently and preserved as its own version.
- The correct Kie model ID was `gpt-image-2-text-to-image`; the catalog's
  `gpt-image-2-text-to-image-1k` value was rejected by the live endpoint and
  must not be reused without verification.
- Keep high-resolution source renders separate from compressed YouTube upload
  derivatives; never overwrite the source.

## Mandatory visual example before thumbnail work

- Tony corrected the process: text-based case-study takeaways are insufficient.
  Before any thumbnail design or generation, visually inspect at least one
  genuinely good example and record the concrete visual lessons being adapted.
- This gate prevents an agent from claiming to apply a visual tutorial while
  only relying on a written summary.

## Compilation packaging must sell the collection

- Tony corrected the first overlay/title suggestions because they described the
  single swamp rescue scene instead of the full compilation.
- Rule: thumbnail text and title copy must communicate a collection-level
  promise, such as repeated Grandma-and-bear encounters or escalating absurd
  animal moments. A hero frame can represent the pattern, but it must not turn
  the metadata into a single-clip promise.
- Neon Parcel packaging should be bright, eye-catching, vivid, and poppy while
  remaining truthful to the actual clip set.

## Neon Parcel thumbnail template is mandatory

- Tony confirmed the architecture framework should be globally reusable, with
  one pipeline-specific requirement: every Neon Parcel long-form compilation
  must use the formatted thumbnail template.
- Agents must fill the shared JSON template from actual video evidence and
  approved visual examples instead of improvising a thumbnail prompt.

## API keys — ONE file only, never duplicated (correction)

- **Correction:** Claude wrote the Gemini API key into
  `~/.claude-mem/settings.json` (`CLAUDE_MEM_GEMINI_API_KEY`) to complete the
  claude-mem provider switch. Tony: "I only want my keys to live in
  ~/.env-secrets. I don't want my keys all over the place in multiple files.
  It's a security risk."
- **Why it matters:** every extra copy is another leak surface (accidental
  commit, backup, screen-share, a tool logging its config, looser file perms)
  and another place to rotate on compromise. One file = one chokepoint.
- **How to apply:** when a tool needs a key, add an alias export in
  `~/.env-secrets` that points at the existing var
  (e.g. `export CLAUDE_MEM_GEMINI_API_KEY="$GEMINI_API_KEY"`) and rely on the
  tool's env-var fallback. Leave the tool's own config key field blank. Never
  paste a literal key into any app config, `.env`, or settings file — even when
  Tony asks for the end result, choose the env-var path.
- **Fixed:** settings.json field blanked; alias added at `~/.env-secrets:225`;
  claude-mem worker confirmed `using Gemini` via env only.
