# 2026-08-28 Feedback

## Shot 1 Voice Interpretation

- Tony confirmed the Shot 1 prompt correctly assigned the line “Ma'am, you
  know that's a wild bear” to the off-camera officer.
- “Grandma calmly replies” was intentionally sufficient context for the older
  woman's response; do not treat this as a prompt-writing error yet.
- The generated result incorrectly gave the line to the wrong-sounding voice
  and repeated the dialogue. Revisit voice attribution, age/character voice,
  and duplicate-dialogue prevention after the remaining shot review.
- Do not revise the prompt or pipeline until Tony completes review of the other
  shots.

## Neon Parcel Storyboard Template

- Tony approved the demonstrated storyboard layout for Neon Parcel only; the
  shared Storyboard Generation skill must remain unchanged.
- Each frame uses a true 16:9 image area with a white band underneath. Frame
  numbers and brief descriptions belong in the band, never inside the image.
- The storyboard is a visual continuity reference, not a literal panel layout
  for the generated video.

## Detailed Prompting Lesson

- Tony approved the revised storyboard after the prompt explicitly separated
  fixed visual context from frame-by-frame action.
- Future Neon Parcel storyboard prompts should specify each frame's action,
  gaze, posture, object position, and state change, while explicitly stating
  what must remain unchanged and what must not happen. Detail should reduce
  ambiguity without turning the storyboard into a literal layout for the video.

## Revision Review Findings

- Shot 2: the alligator morphs when turning instead of physically rotating;
  future prompts and references must preserve the animal's identity and body
  continuity through directional changes.
- Shot 3: no end frame was supplied; it used start-frame-only generation. The
  model still invented a second identical car, driver, and bear, so subject
  count and single-event termination need stronger controls and post-generation
  QA.
- Shot 4: the reference already showed the woman facing the bear, making the
  neighbor's “turn around” instruction causally wrong. The setup must establish
  that she is unaware of the bear before the off-camera warning.

## Shot 3 Directional-Motion Test

- Tony confirmed the corrected Seedance 2 Mini test kept one car moving
  completely through and out of frame, which is good.
- The car still did not match the storyboard's travel direction. Treat this as
  evidence that a storyboard reference alone does not reliably lock vehicle
  direction. Future prompts should explicitly define the vehicle's front and
  rear, lane relationship, entry edge, exit edge, and continuous direction;
  post-generation QA must compare the actual travel direction against the
  storyboard.

## Neon Parcel Default Route Decision

- Tony provisionally graded the storyboard -> Seedance 2 Mini 480p -> Topaz 2x
  -> FFmpeg 1920x1080 process as 89 (B+), based on direct review rather than
  analytics. The prior mixed-generation process was graded C-.
- Lock this route as Neon Parcel's default video-generation process. Keep
  Seedance 1.5 as an explicit fallback/comparison route only; do not silently
  select it for simple shots.

## Iterative Production Framework

- Tony wants a durable, channel-agnostic framework describing how he builds
  production systems: build, test, observe, diagnose, change the smallest
  necessary thing, and test again.
- The framework should be available for agents to consult without overriding a
  pipeline's tailored behavior. It should explain intentional tool selection,
  approval calibration, saved prompts and inputs, exact failure diagnosis,
  non-destructive versioning, and gradual promotion from manual work to
  supervised autonomy.
- This is currently documented as a working architecture specification, not
  promoted to an active skill until it has been reviewed and stabilized.
