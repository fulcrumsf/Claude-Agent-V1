# Feedback Loop: 2026-05-03

## Research Is Its Own Folder
- **Preference**: Tony wants a real `Research` folder for benchmark captures, channel studies, market/product research, comparisons, and analysis notes.
- **Clarification**: Broad research is not a reason to dump files into `Docs`; the primary intent still determines routing.
- **Routing Rule**: Channel screenshots that exist to study a TikTok or YouTube example, such as `Virality-Structures-2`, belong in `Research`.

## Video Package Naming Must Be Descriptive
- **Preference**: Tony does not want video packages left with opaque hash-style names.
- **Rule**: Video ingests should rename the MP4 and keep the folder, transcript scaffold, and tutorial scaffold on the same descriptive stem.
- **Naming Style**: Use `Title-Case-With-Dashes` with acronyms preserved in uppercase, such as `AI-Epoxy-Time-Lapse-Claude-Code-Skill`.

## Screenshot Renaming Uses Vision, Not OCR
- **Preference**: Tony does not want OCR to be the default screenshot rename strategy.
- **Rule**: Use Gemini vision first, then OpenAI vision fallback if needed.
- **Reason**: Renaming should rely on visual interpretation of the screenshot, not text extraction as the primary method.

## Image Notes Belong In Category Folders
- **Preference**: Tony wants the raw image to stay in `Visual_Assets/` while the note file is created directly in the matching category folder.
- **Rule**: Do not create a separate `Asset_Notes/` directory unless Tony explicitly asks for it.
- **Reason**: This keeps the notes with the category taxonomy instead of introducing another parallel storage layer.

## Video Ingest Should Use the Direct Script Path
- **Preference**: Tony wants the stock `process_video_ingest.py` path used directly for video ingest.
- **Rule**: Do not pre-rename or wrap the source video before running the ingest script unless the workflow explicitly requires it.
- **Reason**: The direct script reliably packages the video, keyframes, transcript, and tutorial scaffold, while wrapper-based pre-processing can create recovery problems if a run aborts.

## Failed Wrapper Pass Can Lose Source Clips
- **Observation**: The earlier wrapper-based video pass left `003_Coke-Edit.mp4` and `IMG_9650.MOV` unrecoverable from the workspace.
- **Implication**: Future video ingest should avoid pre-processing wrappers that mutate the source path before the stock ingest script runs.

## Never Trash Source Files
- **Preference**: Tony does not want source files thrown in the trash as part of normal workspace operations.
- **Rule**: Preserve the source until routing is explicit; if something is no longer active, move it only to an approved existing destination or leave it in place and ask.
- **Reason**: Destructive cleanup can permanently lose unrecoverable files and breaks trust in the ingest process.

## Vision Must Match Visible Text
- **Observation**: The `Kronos Time Series Foundation Model` note was a mismatch. The actual image was a YouTube search/results page for EmDash, WordPress, and Cloudflare.
- **Failure Mode**: The error likely entered during vision extraction in `process_image_ingest.py`, where the model hallucinated a model-related label that was not visible in the screenshot.
- **Rule**: For screenshots, title/category text must be grounded in visible words and entities. If the output names entities not present in the image, reject it and re-run or mark it undetermined.

## Single Shared Secrets File
- **Preference**: Tony wants one global secrets file and does not want workspace instructions pointing at a nonexistent `~/.mcp-secrets.env`.
- **Rule**: Use `~/.env-secrets` as the single shared secrets source of truth for active docs and workflows.
- **Reason**: Prevents agents from chasing a stale filename and keeps the secrets path consistent across terminals and coding harnesses.

## Shared Bootstrap Should Cover Obsidian Terminal Too
- **Preference**: Tony uses the terminal inside Obsidian in addition to Mac Terminal, Warp, and VS Code.
- **Rule**: The shared bootstrap should detect or support Obsidian terminal sessions alongside the other shell environments.
- **Reason**: Keeps one bootstrap flow usable across every place Tony launches shell-based agent work.

## Zshrc Wiring Must Be Non-Destructive
- **Preference**: Tony wants shell-file changes backed up before editing.
- **Rule**: Create a timestamped backup in `~/.zshrc-backups/` before wiring `~/.zshrc` to a shared bootstrap.
- **Reason**: Preserves a time-machine-style rollback path if the bootstrap needs to be undone.

## Terminal Runtime Detection Falls Back to a Prompt
- **Preference**: Tony wants exact CLI names to select the runtime automatically, but a fallback prompt when the bootstrap cannot tell which agent is being used.
- **Rule**: `claude`, `codex`, `gemini`, and `agy` should map directly; only generic fallback paths should ask which runtime is active.
- **Reason**: Avoids unnecessary prompts during normal terminal use while still handling ambiguous cases.

## Bootstrap Needs a Home Symlink to Work Everywhere
- **Preference**: Tony wanted the shared bootstrap to load from `~/.agent-bootstrap.sh` in a non-destructive way.
- **Rule**: Keep the canonical file in the workspace, but expose it at the home path via a symlink so shell startup can source it reliably.
- **Reason**: The shell can only source what exists at the expected path; the symlink keeps one source of truth while making the bootstrap visible everywhere.

## UGC Recruitment Ads Are Research, Not Models
- **Preference**: Tony’s `UGCcreator.com` note was correct in content but misfiled under `Models`.
- **Rule**: UGC creator recruitment ads and agency outreach screenshots should route to `Research` when they are being saved as market or offer references.
- **Reason**: They are business/recruitment references, not AI models.

## Future Context Estimator
- **Interest**: Tony may want a rough local utility later to estimate how much startup context is consumed by injected files, hooks, and memory.
- **Rule**: Treat it as a future build idea, not a current task.
- **Reason**: Helpful for managing context pressure across Claude Code, Codex, and Gemini sessions.
