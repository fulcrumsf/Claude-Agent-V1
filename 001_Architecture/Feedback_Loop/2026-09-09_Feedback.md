# 2026-09-09 Feedback

## Preferences confirmed in the September 8–9 reframer session

- Tony's workflow is interview first, plan second, explicit approval third,
  execution fourth. Answers to preference questions do not authorize execution.
- For this subject-aware reframer, keep a separated interaction group visible by
  fitting the wider scene over a blurred background when it cannot fit 9:16.
- Intended use is Tony's own local Agent-OS workflow. Distribution/commercial-app
  integration is not in the current scope. Gate 2 authorizes only the reviewed
  dependency set and Part Three detection/tracking diagnostics; Gate 3 is pending.

---

## Resource Library thread (Claude, Sep 8–9)

### API keys — ONE file only, never duplicated
- **Correction:** Claude wrote the Gemini key into `~/.claude-mem/settings.json` to finish a
  provider switch. Tony: "I only want my keys to live in ~/.env-secrets... It's a security risk."
- **Why:** every extra copy is another leak surface + another thing to rotate on compromise.
- **How to apply:** even when Tony asks for the finished result, use an alias export in
  `~/.env-secrets` (`export TOOL_KEY="$GEMINI_API_KEY"`) + the tool's env-var fallback. Leave the
  tool's own key field blank. Never paste a literal key into any config/.env/settings file.

### Brainstorming style — iterate, don't questionnaire
- Tony rejected the AskUserQuestion multiple-choice flow: "they're too narrow... not one-size-fits-all."
- **How he wants it:** free-form dialogue — "I ask you a question, you answer, I ask more, you answer."
  He explicitly says when he's ready for clarifying questions. Give real opinions/analysis to push
  against, not option menus. Don't jump to "solution A/B/C" — he'll say when.

### Don't over-explain / answer only what's asked
- Repeated correction this session: when Tony asks a narrow question ("are these 5 redundant?"),
  answer THAT, not a full breakdown of adjacent findings. "I don't want a breakdown of all this
  other stuff that I'm not asking for."

### Confirmed decisions (no pushback = validated)
- Image co-location: image beside its note, flat folders, same name stem — "yes I agree."
- Dedup on ingest: image hash + URL match + title soft-flag — "I like your updated logic."
- Folder clutter in Obsidian is a non-issue — Tony doesn't browse in Obsidian, dislikes the UI.
- Deletions staged to `~/Desktop/Delete/` (never hard `rm`); Tony deletes when he's confident nothing's missing.
